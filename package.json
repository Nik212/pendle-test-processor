{
  "private": true,
  "name": "position-pendle-eth-test",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "sentio build",
    "gen": "sentio gen",
    "test": "sentio test",
    "upload": "sentio upload",
    "postinstall": "sentio gen",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@resolv/common": "1.0.0"
  }
}
